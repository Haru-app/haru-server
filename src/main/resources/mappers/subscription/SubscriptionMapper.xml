<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.haruapp.subscription.mapper.SubscriptionMapper">

    <select id="findByUserId" resultType="com.example.haruapp.subscription.domain.Subscription">
        SELECT *
        FROM SUBSCRIPTION
        WHERE USER_ID = #{userId}
    </select>

    <insert id="insertSubscription">
        INSERT INTO SUBSCRIPTION (SUBSCRIPTION_ID, USER_ID, BILLING_KEY, STARTED_AT, EXPIRES_AT, NEXT_PAYMENT_AT)
        VALUES (SUBSCRIPTION_SEQ.NEXTVAL, #{userId}, #{billingKey}, #{startedAt}, #{expiresAt}, #{nextPaymentAt})
    </insert>

</mapper>